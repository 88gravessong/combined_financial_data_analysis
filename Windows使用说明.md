# 💻 Windows系统使用说明

## 🚀 快速开始

### 方法一：一键安装启动（推荐新手）

1. 双击运行 `install_and_start.bat`
2. 脚本会自动：
   - 检查Python环境
   - 创建虚拟环境
   - 安装所有依赖
   - 启动应用程序
   - 打开浏览器

### 方法二：标准启动

1. 双击运行 `start.bat`
2. 等待启动完成
3. 在浏览器中访问 `http://localhost:8080`

### 方法三：快速启动（已配置环境）

1. 双击运行 `quick_start.bat`
2. 直接启动应用

## 📋 启动脚本详解

### 🎯 install_and_start.bat - 一键安装启动
**适用场景**: 首次使用，完全自动化配置

**功能特点**:
- ✅ 自动检查Python环境
- ✅ 创建和配置虚拟环境
- ✅ 升级pip到最新版本
- ✅ 安装项目依赖
- ✅ 验证安装结果
- ✅ 可选创建桌面快捷方式
- ✅ 自动打开浏览器
- ✅ 详细的状态反馈

### 🔧 start.bat - 标准启动脚本
**适用场景**: 日常使用，包含完整检查

**功能特点**:
- ✅ 检查Python环境
- ✅ 验证必要文件
- ✅ 自动配置虚拟环境
- ✅ 安装/更新依赖
- ✅ 启动应用

### ⚡ quick_start.bat - 快速启动脚本
**适用场景**: 环境已配置，需要快速启动

**功能特点**:
- ✅ 激活虚拟环境
- ✅ 直接启动应用
- ✅ 最快启动速度

### 🛠️ start_dev.bat - 开发模式启动
**适用场景**: 开发调试，需要详细信息

**功能特点**:
- ✅ 启用Flask调试模式
- ✅ 安装开发工具
- ✅ 显示环境信息
- ✅ 自动重载功能

## 🔧 系统要求

### 必需软件
- **Python 3.8+** (推荐3.9或更高版本)
- **Windows 10/11** (支持Windows 7/8，但推荐更新版本)

### Python安装
1. 访问 [Python官网](https://www.python.org/downloads/)
2. 下载最新版本的Python
3. 安装时**务必勾选**"Add Python to PATH"
4. 验证安装：打开命令提示符，输入 `python --version`

## 📁 目录结构

启动后会自动创建以下目录：
```
项目根目录/
├── venv/                 # Python虚拟环境
├── logs/                 # 日志文件目录
├── temp/                 # 临时文件目录
├── uploads/              # 上传文件目录
└── 财务数据分析系统.lnk   # 桌面快捷方式（可选）
```

## 🎮 使用步骤

### 第一次使用
1. 解压项目文件到任意目录
2. 双击运行 `install_and_start.bat`
3. 按提示选择是否创建桌面快捷方式
4. 等待浏览器自动打开
5. 开始使用系统

### 日常使用
1. 双击桌面快捷方式 或 运行 `quick_start.bat`
2. 等待启动完成
3. 在浏览器中使用系统

### 停止服务
- 在命令行窗口按 `Ctrl+C`
- 或直接关闭命令行窗口

## 🚨 常见问题解决

### 问题1: "python不是内部或外部命令"
**原因**: Python未安装或未添加到PATH环境变量

**解决方案**:
1. 重新安装Python，勾选"Add Python to PATH"
2. 或手动添加Python到系统PATH：
   - 右键"此电脑" → "属性" → "高级系统设置"
   - "环境变量" → 在"系统变量"中找到"Path"
   - 添加Python安装目录（如：`C:\Python39\` 和 `C:\Python39\Scripts\`）

### 问题2: 脚本运行出现乱码
**解决方案**:
- 脚本已自动设置UTF-8编码
- 如仍有问题，请确保系统区域设置为中国

### 问题3: 虚拟环境创建失败
**可能原因**:
- 磁盘空间不足
- 权限不够
- 防病毒软件拦截

**解决方案**:
1. 以管理员身份运行脚本
2. 检查磁盘空间
3. 临时关闭防病毒软件
4. 手动创建虚拟环境：`python -m venv venv`

### 问题4: 依赖安装失败
**解决方案**:
1. 检查网络连接
2. 使用国内镜像源：
   ```cmd
   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
   ```
3. 使用可信主机参数：
   ```cmd
   pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org -r requirements.txt
   ```

### 问题5: 端口8080被占用
**检查端口占用**:
```cmd
netstat -ano | findstr :8080
```

**解决方案**:
1. 结束占用进程
2. 或修改 `app.py` 中的端口号

### 问题6: 浏览器无法访问
**检查事项**:
1. 确认服务器已启动（命令行显示"Running on http://..."）
2. 尝试访问 `http://127.0.0.1:8080`
3. 检查Windows防火墙设置
4. 尝试其他浏览器

## 🔐 安全注意事项

1. **虚拟环境**: 脚本会自动创建虚拟环境，避免污染系统Python环境
2. **临时文件**: 上传的文件会在处理后自动清理
3. **网络访问**: 默认只监听本地地址(127.0.0.1)，外网无法直接访问
4. **文件权限**: 确保项目目录有适当的读写权限

## 📞 技术支持

如果遇到其他问题：

1. **查看日志**: 注意命令行窗口中的错误信息
2. **检查文件**: 确保所有必要文件都存在
3. **重新安装**: 删除venv目录后重新运行 `install_and_start.bat`
4. **系统兼容性**: 确保Windows版本和Python版本兼容

## 🎉 成功标志

当看到以下信息时，说明启动成功：
```
🚀 启动Web服务器...
📊 访问地址: http://localhost:8080
💡 使用 Ctrl+C 停止服务器

 * Running on http://127.0.0.1:8080
 * Debug mode: off
```

此时可以在浏览器中访问系统开始使用！

---

**版本**: Windows v1.0  
**更新时间**: 2024年12月  
**兼容性**: Windows 7/8/10/11 + Python 3.8+